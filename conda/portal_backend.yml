name: portal_backend
channels:
    - conda-forge
    - nodefaults
dependencies:
    - rust
    - uvicorn
    - fastapi[standard]
    - pymongo
    - nodejs # for installing npm indirectly
    - tmux
    - pip
    - pip:
        - fastapi-cache2
